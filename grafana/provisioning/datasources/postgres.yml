apiVersion: 1

datasources:
  - name: NEXUS PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: nexus
    user: nexus
    secureJsonData:
      password: nexus2024
    jsonData:
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 5
      connMaxLifetime: 14400
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
    editable: true
